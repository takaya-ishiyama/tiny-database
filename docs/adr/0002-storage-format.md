## バイトオーダー
Little-endian
理由：多くのPCアーキテクチャ（x86, x86-64など）がLittle-endianを採用しているため、互換性を確保するため。

## ページサイズ
4 KiB
理由：一般的なPCのページサイズに合わせることで、メモリマッピングやキャッシュ効率を向上させるため。

## 最大key/valueサイズ
key: 1 KiB
value: 1 MiB

理由：keyサイズはページサイズを超えないように制限し、valueサイズは大きなデータを扱えるようにするため。
